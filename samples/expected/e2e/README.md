# E2E Expected Outputs

This directory describes expected output shapes for automated knowledge-loop checks.

The current Phase 1 acceptance suite validates the frontend mock loop. Later backend and adapter slices should add deterministic expected Markdown, graph, vector, and Ask evidence fixtures here.
