# Converter

Converter integration placeholder.

Atlas should wrap internal tools through adapters:

- `trinity-office` for Office-to-PDF conversion.
- `document-normalize` for PDF-to-Markdown and image extraction.

Do not call converter or parser tools directly from product workflows. Keep parser and converter choices configurable.
