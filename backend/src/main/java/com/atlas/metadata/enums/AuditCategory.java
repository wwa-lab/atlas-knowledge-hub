package com.atlas.metadata.enums;

/** Product audit event categories for governance filtering. */
public enum AuditCategory {
  AUTH,
  MEMBERSHIP,
  REVIEW,
  PUBLISH,
  WIKI,
  GRAPH,
  ASK,
  MODEL,
  ADAPTER,
  SETTINGS
}
