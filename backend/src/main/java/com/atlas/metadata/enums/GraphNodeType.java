package com.atlas.metadata.enums;

/** Deferred graph node type persisted for later graph slices. */
public enum GraphNodeType {
  KNOWLEDGE_SPACE,
  DOCUMENT,
  WIKI_PAGE,
  CONCEPT,
  ENTITY,
  SOURCE_CHUNK
}
