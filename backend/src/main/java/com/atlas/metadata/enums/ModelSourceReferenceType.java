package com.atlas.metadata.enums;

/** Source reference categories accepted by model run evidence. */
public enum ModelSourceReferenceType {
  FILE_ITEM,
  SOURCE_CHUNK,
  WIKI_PAGE,
  GRAPH_NODE,
  ASK_CONTEXT
}
