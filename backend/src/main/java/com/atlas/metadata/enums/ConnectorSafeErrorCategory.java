package com.atlas.metadata.enums;

/** User-safe connector item error categories. */
public enum ConnectorSafeErrorCategory {
  NONE,
  VALIDATION,
  CONNECTOR_UNAVAILABLE,
  UNSUPPORTED_SOURCE,
  SOURCE_UNREADABLE,
  SAFE_SYSTEM
}
