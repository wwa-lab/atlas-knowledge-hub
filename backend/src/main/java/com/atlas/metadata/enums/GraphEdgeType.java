package com.atlas.metadata.enums;

/** Deferred graph edge type persisted for later graph slices. */
public enum GraphEdgeType {
  CONTAINS,
  DERIVED_FROM,
  MENTIONS,
  DEFINES,
  RELATED_TO,
  BELONGS_TO,
  USES,
  DEPENDS_ON,
  REVIEWED_BY
}
