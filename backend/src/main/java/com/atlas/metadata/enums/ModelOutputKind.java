package com.atlas.metadata.enums;

/** Safe output descriptor kinds produced by model adapters. */
public enum ModelOutputKind {
  TEXT_SUMMARY,
  EMBEDDING_METADATA,
  RERANK_SCORES,
  VISION_SUMMARY,
  SPEECH_SUMMARY,
  ERROR
}
