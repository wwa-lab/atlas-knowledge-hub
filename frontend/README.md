# Frontend

Frontend implementation placeholder.

The MVP starts with `prototypes/index.html`, a no-build static prototype. Do not introduce a framework until the product flow is validated and explicitly requested.
