# Frontend

Frontend implementation placeholder.

The MVP starts with `prototypes/index.html`, a no-build static prototype.

Selected frontend direction after prototype validation:

- Vue 3.
- Vite.
- TypeScript.

Do not introduce the frontend framework until the product flow is validated and explicitly requested.
